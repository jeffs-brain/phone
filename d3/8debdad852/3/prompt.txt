In /Users/alexjay/code/jeffs-brain/phone, implement the chat UI/composer side of the UX. You are not alone in the codebase; do not revert others' edits, and keep your write scope to app/index.tsx only. Assume the store will expose stagedAttachments, stageAttachment, clearStaged, removeStagedAttachment(index), appendThinkingChunk/finishThinking via Message.thinking, where thinking can be a structured object with text/status. Goals: render assistant thinking collapsed by default with tap-to-expand detail, keep visible answer separate, fix Send active/disabled styling and enable Send for draft text OR staged attachments, add image picking with expo-image-picker, show staged image/file chips/previews with remove controls, and keep the composer usable on iPhone 17 Pro Max without overlap. If file/document picking needs a new dependency, do not add one; add a disabled/placeholder attachment affordance or implement image-only plus clearly structure the UI for future files. Keep TypeScript strict, no any, British English. Run `bun run typecheck --pretty false` before final if possible. In your final, list changed files and validation output.